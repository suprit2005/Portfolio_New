{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd portfolio_site && python manage.py collectstatic --no-input"
  },
  "deploy": {
    "startCommand": "cd portfolio_site && python manage.py migrate --no-input && gunicorn portfolio_site.wsgi:application --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
